\def\ops{16000}

\documentclass{standalone}

\usepackage{pgfplots}
\usepgfplotslibrary{statistics}

\begin{document}
  \begin{tikzpicture}
    \begin{semilogxaxis}[title={Throughput for different max levels: lazy lock, size \ops},              % Title of the graph
                 xtick={1,2,4,8,16,32,64,128,256},  % The ticks on the x-axis
                 xlabel={number of threads},        % Label of the x-axis
                 ylabel={Kops/s},             % Label of the y-axis
                 legend style={
                   at={(1.05,0.95)},                % Position of the legend anchor
                   anchor=north west                % The legend anchor
                 },
                 log basis x={2},
                 log ticks with fixed point,]

      % Each graph is added via \addplot and a .data file, and
      % add a legend entry for the previous graph.
      \addplot table {\DATAPATH/throughput/smallbench_\ops_3_l.data};
      \addlegendentry{3 levels}
      \addplot table {\DATAPATH/throughput/smallbench_\ops_5_l.data};
      \addlegendentry{5 levels}
      \addplot table {\DATAPATH/throughput/smallbench_\ops_10_l.data};
      \addlegendentry{10 levels}

    \end{semilogxaxis}
  \end{tikzpicture}
\end{document}
